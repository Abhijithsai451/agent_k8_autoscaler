apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gateway
  namespace: agent
spec:
  rules:
    - host: gateway.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 8000
